services:
  - type: web
    name: av-ai-api
    runtime: python
    rootDir: central-api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
